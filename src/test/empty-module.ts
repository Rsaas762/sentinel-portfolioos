// Stub used by Vitest to satisfy `import "server-only"` in the node test
// runner. In `next build`, the real `server-only` guard still applies via the
// react-server export condition — this only affects unit tests.
export {};
